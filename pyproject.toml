[project]
name = "infra-audit"
version = "0.1.0"
description = "Infrastructure Audit CLI"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "click>=8.3.1",
]

[project.scripts]
infra_audit = "infra_audit.cli:main"
